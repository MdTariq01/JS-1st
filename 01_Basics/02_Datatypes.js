"use strict"; // using this it will treat all js code as newer version

// alert("tariq") // will not work coz it's not in browser

console.log(3+2)
console.log("Tariq")

let name = "Tariq"
let age = 19
let isLoggin = true
let temperature = null // yup null is also a data type in js 
// for eg. if there is an error then it will senf 0 but it's a value so we will use null

// number
// bigInt
// String = ""
// boolean = true / false
// null = standalone value
// undefined
// symbols => uniqness

// const mysybol = Symbol("Tariq") // way to define symbol

console.log(typeof "Tariq")
console.log(typeof 234)
console.log(typeof True)

console.log(typeof null) // null is of type Object 
console.log(typeof undefined) // undefined type is undefined

console.log(typeof(null));

console.log(mysybol);
