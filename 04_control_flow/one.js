// if (condition) {
    
// }

// if (condition) {
    
// } else {
    
// }

// switch case syntax 
const month = "sept";

// switch case syntax

// switch (key) {
//     case value:
        
//         break;

//     default:
//         break;
// }

const name = "Luffys";
switch (name) {
    case "Tariq":
            console.log(`${name} just loggedin`);
        break;

    case "Luffy":
            console.log(`${name} just loggedin`);
        break;

    case "Zoro":
            console.log(`${name} just loggedin`);
        break;

    default:
        console.log("Please enter name");
        break;
    }